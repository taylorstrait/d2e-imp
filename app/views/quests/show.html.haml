%h2= @quest.name

%h4 Description


%h4 Encounters
- for encounter in @quest.encounters
  = link_to encounter.name, encounter

%h4 Reviews

%h4 Discussion

- if user_signed_in? && current_user == @quest.user
  = link_to "New Encounter", new_encounter_path(:quest_id => @quest.id)
  = link_to t('.edit', :default => t("helpers.links.edit")), |
    edit_quest_path(@quest), :class => 'btn'           |